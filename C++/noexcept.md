`noexcept` 能加速，但别乱加。只有当你百分之百确定以下两点时才加：

1. 函数内部没有 `throw`。
    
2. 函数内部调用的其他函数也都是安全的。
    

**最建议加 `noexcept` 的地方：**

- **移动构造函数**（必加，为了让容器加速）。
    
- **移动赋值运算符**。
    
- **析构函数**（C++ 默认析构函数就是 `noexcept` 的，千万别在析构里抛异常！）。
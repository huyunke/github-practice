`noexcept` 能加速，但别乱加。只有当你百分之百确定以下两点时才加：

1. 函数内部没有 `throw`。
    
2. 函数内部调用的其他函数也都是安全的。
    

**最建议加 `noexcept` 的地方：**

- **移动构造函数**（必加，为了让容器加速），如果编译器得到了这个保证，它在处理容器（如 `std::vector`）扩容时，就会大胆地使用**移动（Move）而不是**拷贝（Copy）**，从而大幅提升性能
    
- **移动赋值运算符**
    
- **析构函数**（C++ 默认析构函数就是 `noexcept` 的，千万别在析构里抛异常！）
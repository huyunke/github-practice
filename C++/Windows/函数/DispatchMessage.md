- **它的职责**：它查看 `MSG` 结构体中的 `hwnd`（窗口句柄），然后告诉系统：“请调用这个窗口对应的 `WndProc` 回调函数，把这封信交给他。”
    
- 你并不需要手动调用 `WndProc`。`DispatchMessage` 会通过操作系统间接地触发你写的那个巨大的 `switch-case` 函数
    
- **如果没有它**：你的 `WndProc` 永远不会被执行，窗口就像失去灵魂的躯壳，对任何点击或重绘请求都没有反应
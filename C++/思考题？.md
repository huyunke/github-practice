- 假设你有一个 `GoldTrophy`（高级奖杯）对象保存在 `std::unique_ptr` 中。现在你想把这个奖杯借给一个 `inspectTrophy(const Trophy& t)` 函数查看一下，但不打算把所有权给它（也就是说，函数看完后，奖杯还得回我手里）
- **你会如何把这个智能指针里的对象传给函数？**

1. 传 `std::move(myTrophy)` 进去
    
2. 传 `*myTrophy` 进去 ✅
    
3. 把智能指针销毁，直接传裸指针

- 假设我们有一个精简版的 `Buffer` 类。请补全移动构造函数的关键一步：
```cpp
class Buffer {
    int* data;
    size_t size;
public:
    // 构造函数
    Buffer(size_t s) : size(s), data(new int[s]) {}

    // 移动构造函数 (&& 代表右值引用)
    Buffer(Buffer&& other) noexcept {
        // 1. 偷走资源
        this->data = other.data;
        this->size = other.size;

        // 2. TODO: 这一步至关重要！如果不做，other 析构时会把 data 也删掉
        // 你应该对 other.data 做什么？
        __________________________ ;
    }

    ~Buffer() { delete[] data; }
};
```
`other.data = nullptr;`
既然在移动构造函数内部要手动置空，那什么时候才在类成员上用 `std::move` 呢？ 答案是：**当类成员本身就是“支持移动”的对象（如 `std::string` 或 `std::vector`）时，我们就不需要进行手动置空，因为这些类自己的移动构造函数已经帮我们处理好了这件事**